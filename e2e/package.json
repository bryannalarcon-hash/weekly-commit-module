{
  "name": "wcm-e2e",
  "version": "0.1.0",
  "private": true,
  "description": "Live end-to-end suite for the Weekly Commit Module: Cypress + Gherkin against the federated host+remote stack, plus a thin Playwright smoke.",
  "type": "module",
  "scripts": {
    "cypress:run": "cypress run --e2e --browser chrome",
    "cypress:open": "cypress open --e2e",
    "playwright:smoke": "playwright test"
  },
  "devDependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^20.1.2",
    "@bahmutov/cypress-esbuild-preprocessor": "^2.2.3",
    "@playwright/test": "1.47.2",
    "cypress": "^13.15.0",
    "esbuild": "^0.23.1",
    "typescript": "^5.5.4"
  }
}
